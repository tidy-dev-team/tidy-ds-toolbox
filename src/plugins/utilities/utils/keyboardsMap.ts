/**
 * Keyboard mapping for Misprint utility — re-exported from the shared single
 * source of truth (`src/shared/misprint.ts`, issue #98). Kept so existing
 * imports keep working; new code should import from `shared/misprint`.
 */
export { keyboardsMap } from "../../../shared/misprint";
