services:
  metabase:
    image: metabase/metabase:latest
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - metabase-data:/metabase.db
    restart: unless-stopped

volumes:
  metabase-data:
